# Dummy Authenticator configuration

# Authenticator

# Authenticator implementation (full qualified class name)
authenticator.class=io.j1st.smartcharger.authenticator.SmartChargerAuthenticator
#authenticator.class=io.j1st.mithqtt.authenticator.power.PowerAuthenticator
#authenticator.class=io.j1st.photovoltaic.authenticator.PhotoVoltaicAuthenticator


# Dummy

# Allow '$' in topic
allowDollar = false

# Topic will be rejected in PUBLISH and SUBSCRIBE
deniedTopic = nosubscribe
# MongoDB storage configuration
mongo.url=mongodb://zenin:zenin@localhost:27017
mongo.database=smartcharger_sj
#mongo.database=photovoltaic
